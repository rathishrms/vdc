{
    "deployment-prefix": {
        "value": "${general.organization-name}-${ENV:ENVIRONMENT-TYPE.deployment-name}"
    },
    "virtualNetwork-rules": {
        "value": "${ENV:ENVIRONMENT-TYPE.diagnostic-storage-account.virtualNetwork-rules}"
    },
    "ip-rules": {
        "value": "${ENV:ENVIRONMENT-TYPE.diagnostic-storage-account.ip-rules}"
    }
}